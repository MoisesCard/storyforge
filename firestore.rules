rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{projectId} {
      allow read, write: if true;  // For development, tighten this for production
    }
    match /documents/{documentId} {
      allow read, write: if true;  // For development, tighten this for production
    }
  }
} 